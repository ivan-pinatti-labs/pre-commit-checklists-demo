[project]
name = "log-rotator-demo"
version = "0.1.0"
description = "Sample log rotation and disk usage scripts for the pre-commit-checklists demo."
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I"]
